# schedutil instead of the kernel default `performance`, which pins every core at its max clock.
# Cannot be a modprobe option: cpufreq is built in and cpufreq.default_governor= is a __setup() parameter.
w /sys/devices/system/cpu/cpu*/cpufreq/scaling_governor - - - - schedutil
