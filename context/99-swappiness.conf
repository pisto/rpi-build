# Bias reclaim toward page cache over swapping out active memory. Sensible
# with or without a swap device present (a no-op if there's no swap at all);
# lower than the kernel default of 60 because swap on this class of board is
# meant as pressure relief, not routine use.
vm.swappiness=10
