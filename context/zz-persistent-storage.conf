# the "zz-" prefix is load-bearing: journald.conf.d drop-ins from /etc and /usr/lib
# are merged in one alphabetical pass by filename regardless of directory, and this
# must sort after raspi-overlayroot's volatile-storage.conf to override it
[Journal]
Storage=persistent
